__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.venv/
venv/
.vscode/
.idea/
.DS_Store
.coverage
htmlcov/

# Review archives built with `git archive`: a snapshot of the repository stored
# inside that repository is redundant the day it is made and wrong after it.
*.zip

# Specimens are the test suite here and DO belong in git - see CLAUDE.md.
# Only genuinely private material is excluded.
private-samples/

# Working notes, not documentation. HANDOFF.md is the running log of what was
# tried and what it cost, and it names files from a sibling checkout that are
# private. CONTRIBUTING.md is my rules for working in this repository. Neither
# is something a reader of the published project needs, and the sibling project
# tracks neither of its equivalents either.
HANDOFF.md
CLAUDE.md
